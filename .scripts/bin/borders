#!/bin/zsh
if [ $# -eq 0 ];then
    echo "No arguments given"
elif [ $# -eq 1 ]; then
    yabai -m config window_border $1
else
    yabai -m config window_border $1
    yabai -m config window_border_width $2
fi
